{
  "name": "millionsweeper",
  "version": "0.1.0",
  "description": "1 million cells Minesweeper Board Renderer",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch",
    "pretest": "tsc --noEmit false --allowImportingTsExtensions false --outDir dist && cp test/stubs/configStub.js dist/config.js && cp test/stubs/renderStub.js dist/gfx/render.js && cp test/stubs/persistStub.js dist/persist.js && cp test/stubs/playersStub.js dist/players.js && cp test/stubs/uiStub.js dist/gfx/ui.js && node test/fix-imports.cjs",
    "test": "node --test"
  },
  "dependencies": {
    "idb-keyval": "^6.2.1",
    "seedrandom": "^3.0.5",
    "simplex-noise": "^2.4.0",
    "three": "^0.150.1"
  },
  "devDependencies": {
    "@types/seedrandom": "^3.0.8",
    "@types/three": "^0.150.2",
    "typescript": "^5.8.3",
    "vite": "^4.3.9"
  }
}
